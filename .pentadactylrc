"1.0

loadplugins '\.(js|penta)$'
group user
map -count -modes=n ; <count>:
map -count -modes=c <C-c> <Esc>
map -count -modes=i <C-c> <Esc>
map -count -modes=i jj <Esc>
map -count -modes=t <C-c> <Esc>
map -count -modes=v <C-c> <count><Esc>
set noinsertmode

" vim: set ft=pentadactyl: